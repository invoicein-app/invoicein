# Copy to .env.local (or export in shell) before running E2E locally.
E2E_TEST_EMAIL=
E2E_TEST_PASSWORD=

# App must be built and running — Playwright starts `npm run start` by default.
# PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
# PLAYWRIGHT_SKIP_WEBSERVER=1
